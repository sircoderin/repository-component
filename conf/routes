# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing a sample home page
GET     /                           controllers.UserController.modifyUser(id: String)
GET     /get                       controllers.UserController.get(id: String)
GET     /save                       controllers.UserController.save()
GET     /createCollection           controllers.UserController.createCollection()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
